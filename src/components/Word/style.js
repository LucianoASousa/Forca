import styled from "styled-components";

export const Container = styled.span`
  color: #d1a436;
  font-size: 1.5rem;
  position: absolute;
  bottom: 15rem;
  left: 50%;
  transform: translateX(-50%);
`