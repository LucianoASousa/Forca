import styled from "styled-components";

export const Container = styled.span`
  color: #61dafb;
  font-size: 1.5rem;
  position: absolute;
  bottom: 12rem;
  left: 50%;
  transform: translateX(-50%);
`